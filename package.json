{
  "name": "mutation-test",
  "scripts": {
    "start": "node src/server.js",
    "test": "npx jest",
    "stryker": "npx stryker run",
    "open:mutation": "node open-report.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^9.0.1",
    "@stryker-mutator/jest-runner": "^9.0.1",
    "jest": "^29.7.0",
    "supertest": "^7.1.1"
  }
}